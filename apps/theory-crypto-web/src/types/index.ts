/**
 * Type re-exports from @dock108/js-core.
 * 
 * This module serves as a centralized type export point for strategy-related types.
 * All types are sourced from the shared js-core package to ensure consistency
 * across the monorepo and avoid duplication.
 */
export type {
  StrategyResponse,
  StrategySaveRequest,
  BacktestRequest,
  BacktestResult,
  AlertEvent,
  ToggleAlertsRequest,
  StrategySpec,
  BacktestBlueprint,
  EdgeDiagnostics,
  AlertSpec,
  StrategyInterpretation,
} from "@dock108/js-core";

