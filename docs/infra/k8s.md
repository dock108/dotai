# Kubernetes (future)

Placeholder for manifests/Helm charts once the stack moves beyond single-node Docker.

## Planned assets
- Base namespace + network policies
- Deployments for each app/service
- Ingress (+cert-manager) for `*.dock108.ai`
- GitOps manifest bundle (Flux/Argo) for Hetzner cluster
