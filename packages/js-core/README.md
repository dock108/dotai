# JS Core

TypeScript utilities shared across the React apps + future browser extensions.

## Contents (planned)
- API SDK (REST client + hooks)
- Validation schemas (zod) matching the backend Pydantic models
- Guardrail helpers (prompt builders, content filters)
- Shared analytics helpers

Package will be published via the monorepo workspace once implemented.
